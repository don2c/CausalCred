# Manuscript summary inputs

This directory contains values transcribed from the evaluated manuscript. The values support table rendering, formula recomputation, and cross-table consistency checks.

Records in this directory use the `manuscript_summary` evidence class. Deterministic trial projections produced from these values are stored in `data/manuscript_derived/`. Executed protocol evidence is stored independently in `data/executed/` and does not consume the summary outcomes.
